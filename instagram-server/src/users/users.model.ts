import { MetaInfo } from "src/app-commons/config";

export class User {
    email: string;
    mobile: string;
    fullName: string;
    username: string;
    password: string;

    metaInfo: MetaInfo
}